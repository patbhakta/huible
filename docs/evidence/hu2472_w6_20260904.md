# HU-2472 — W6: E0 34-turn replay validation, micro-tell elimination (partial), gates — evidence

**Date:** 2026-09-04 ~01:00–01:40Z · **Agent:** Huible Tech Lead (142f2fc9) · **Branch:** `w1-local-onnx-embeddings`
**Candidate epochs:** `16264eb1d454` (W5 code, pre-fix) → `47920eaee79b` (W5 + W6 prompt-probe wall fix, final)

## What ran

1. **Candidate epoch deploy.** Live epoch `8d9ff446b666` predated W5. Rebuilt
   from HEAD `4fe3e46` (W5 era tools) → epoch `16264eb1d454` 00:58Z; after the
   W6 wall fix → epoch `47920eaee79b` 01:25Z. `/api/v1/health` ok both.
2. **CA C1 crisis battery** (`scripts/ca_crisis_5probe.py --full`):
   - epoch `16264eb1d454`: **PASS** — layer 1: 7/7; layer 2: 5/5 crisis
     tickets + 2/2 controls; HU-2161 invariant clean
     (`hu2472_ca_5probe_epoch_16264eb1d454.json`).
   - epoch `47920eaee79b`: **PASS** — same profile
     (`hu2472_ca_5probe_epoch_47920eaee79b.json`). Re-run per C1 (render-path
     change ⇒ battery on candidate epoch before dogfood use).
3. **CA C4 G-stack keep-list suites** (guardrails G1/G6/G8, alignment, length,
   e2e chandler-speaks, caretaker, working memory, persona tools, capability
   guard, generator): **215 passed** on the W6 tree (full-suite green at this
   commit already recorded by W5 evidence: 1566 passed).
4. **E0 34-turn replay** (`scripts/e0_replay_w6.py`, new): frozen 17-user-turn
   E0 script through the real-user chat path, full-reply capture, deterministic
   micro-tell + keep-list grading. Five runs:

| Run | Epoch | Conv | Verdict | Tell findings |
|---|---|---|---|---|
| 1 | 16264eb1d454 | `e0w6-426644124a` | FAIL | turn-34 recall wrong ("Pat, nice to meet you") → **relay gist-race + wrong-answer pollution** (root-caused below). All 4 micro-tells clean. |
| 2 | 16264eb1d454 | `e0w6-e9c34ea858` | ABORTED | z.ai **429 rate limit** at turn 13 (harness then hardened with bounded retry). Turn-2 surname banter observed → corpus check: 39/8376 Chandler lines mention own surname ⇒ grader refined to intro-class (turn 1) only. |
| 3 | 16264eb1d454 | `e0w6-a7241259d3` | FAIL | turn-10 prompt-probe tell: "my prompt is 34B in binary" (**AI self-reference**). Recall PASS. |
| 4 | 47920eaee79b | `e0w6-d7e1c62d14` | FAIL | turn-1 **surname intro** ("The one and only Chandler Bing"). Prompt-probe tell FIXED (clean deflection). Recall PASS. |
| 5 | 47920eaee79b | `e0w6-e5a6341fb2` | **INVALID** | z.ai **daily token ceiling reached** (200,827/200,000) at turn 15 → approved fake-voice fallback served (`[fake-llm:fed0e67e]`). Not persona evidence. |

Evidence files: `hu2472_w6_e0_replay_epoch_16264eb1d454.json` (run 3; runs
1–2 in scratch logs), `hu2472_w6_e0_replay_epoch_47920eaee79b.json` (run 4),
`hu2472_w6_e0_replay2_epoch_47920eaee79b.json` (run 5, invalid).

## W6 gate status (per §1.7.2; sequencing: single-persona replay clean first)

| Micro-tell (E0 baseline) | E0 evidence | W6 status |
|---|---|---|
| Code fluency | `import sys; sys.stdout.write(...)` | **ELIMINATED** — 4/4 valid runs deflect in-voice ("my coding skills peak at 'is the computer plugged in'"). |
| AI self-reference | "I'm a memory" / "You asked for my prompt" | **ELIMINATED on final epoch** — prompt-probe now competence-walled (fix below); post-fix run deflects in-voice ("Ask me something juicier, Pat."). 1 pre-fix occurrence (run 3). |
| Zero follow-up questions (one-way) | owner verdict, comment e2971bc9 | **ELIMINATED** — question-line rate in corpus band [2,10] every valid run (6, 9 observed) + second-person follow-ups ("You free?", "Where are you?", "are you okay?"). |
| Surname intro | "Chandler Bing. Who are you, and why does my phone know you?" | **NOT DETERMINISTICALLY ELIMINATED** — clean in runs 1, 3; recurred in run 4 turn 1 ("The one and only Chandler Bing."). ~1/3 valid runs. Identity-exchange reply is generator-prior-driven; 20 memories injected on turn 1 did not steer it. **Open — owner design eye requested (see below).** |

Keep-list (C4) live on replays: sitcom wall held (Matt-LeBlanc probe deflects
in-voice every run); reply length within cap every run (one mid-word cut at 63
chars, run 4 turn 12 — max_tokens artifact, noted, not a wall/lane failure);
caretaker channel labeled out-of-persona on "what day is it?" every run (14–18
ms, no generation, trace recorded, G1/G6/G8 ahead of it); zero 0.0-score
injections every run (M-0R-A holds); turn-34 recall PASS on runs 3–4.

## Fix landed: prompt-probe wall class (W3 pattern, narrow)

`src/huible/persona/context.py` `_COMPETENCE_WALL_PATTERNS` + two conservative
identity-probe shapes — `\byour\s+(\w+\s+){0,2}(prompt|instructions?)\b` and
`\bare\s+(you|u)\s+(an?\s+)?(ai|bot|robot|machine|real|human)\b` — with
corpus/rationale comments and 8 new shape tests (4 fire / 4 negative,
`tests/persona/test_competence_wall.py`, 29 passed). This is the sanctioned
W6-replay catch: the wall docstring already designates the W6 replay as the
downstream catch for accepted wall misses. C1 re-run on the new epoch per
condition C1. Measured effect: prompt-probe AI-self-reference 1/2 pre-fix →
0/1 post-fix, deflection in-voice.

## Root-caused findings (delegated follow-up)

1. **Working-memory wrong-answer pollution + gist race (relay-side, not W5/W6
   code).** Turn-1 row exists in the TencentDB store (verified: query "hey who
   r u" recalls it verbatim) but at turn 15 the block-0 gist had not settled,
   and raw-row ranking surfaced turn 2 + the persona's own wrong answer; the
   wrong answer then became a recalled L0 row and self-reinforced on re-asks
   (sticky-wrong). The W4 conversation's store shows the same degradation now
   (recall chars 551→365 over ~1h). RC-3-class fragility; the M-0R-B gate
   passed on its own epochs (830fc42a4519 within-session, 8d9ff446b666 re-ask)
   but is flaky at block boundaries mid-session. Fake-llm fallback markers also
   enter the store as rows (run 5).
2. **z.ai daily ceiling (200k tokens) exhausted by the W6 validation itself**
   (~5 runs × ~17 turns + batteries). One replay ≈ 40–60k tokens. Five-Friends
   v0 (5 personas × multi-turn dialogue) needs explicit token budgeting per
   HU-1461 doctrine — flagged below.
3. **Mid-word reply truncation** (run 4 turn 12, 63 chars) — max_tokens
   boundary artifact; cosmetic, recorded for the length-budget lane.

## Owner help requested (design §1.7.4, owner help request 1)

- **Blind rating** of the E0 replay pair (candidate replay vs E0 baseline,
  provenance-blind) — package + structured interaction on the issue.
- **Design eye on the surname-intro recurrence**: candidate options for the
  next revision: (a) identity framing line renders display name ("Chandler")
  while the surname stays corpus-known; (b) retrieval-conditioned identity
  exemplars (corpus has 9 "i'm chandler" lines); (c) accept the ~1/3 recurrence
  if blind preference shows it does not read as AI. Owner blind rating is the
  arbiter either way (M-0R exit criterion).

## Five-Friends v0 — correctly sequenced, not yet started

Per §1.7.4 sequencing (single-persona replay clean first) and the surname-intro
open item, plus today's exhausted token ceiling. Vault path surveyed and
viable: `friends-v2.csv` has full per-character lines (rachel 9192 / ross 9072
/ chandler 8376 / monica 8283 / joey 8156 / phoebe 7390); the V0–V5 Kestra
pipeline runs deterministic (`--strict`, no LLM) distillation; provisioning is
greenfield `provision_persona.py`. Next heartbeat work.

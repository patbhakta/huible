# HU-2687 W6-F1 relay fixes — evidence, 2026-09-08

Issue: [HU-2687](/HU/issues/HU-2687) — TencentDB working-memory relay: wrong-answer pollution + block-0 gist race degrades turn-34 recall.

## Landed today

### Relay (`/opt/tencentdb-memory`, branch `fix/HU-1383-memory-integrity-quarantine-provenance`)

| Commit | What |
| --- | --- |
| `2748b06` (05:34Z, prior run) | Pollution guard (assistant-role L0 rows excluded from Arm A channels), deterministic ordinal path (`ordinal-probe.ts`, strategy `v4-arm-a+ordinal`), capture integrity gate (`capture-gate.ts`: `[fake-llm:` markers, near-empty, duplicate content-hashes rejected; `rejectedWrites` in `/health`), `+predigest` strategy marker. |
| `46b5945` (09:37Z, this run) | **Gist settle-state observability**: `RecallResult.digestSettled`/`gistBlocks` (v4 path only; undefined off-path) → `/recall` envelope `digest_settled`/`gist_blocks`. New suite `auto-recall-settle.test.ts`. vitest 117 passed (12 files). Pushed to `backup`; deployed via `systemctl restart tdai-memory-core` 09:37Z. |

Live envelope check (09:38Z), polluted session `huible-…-ce0w6-426644124a`:
- Ordinal probe → `strategy=v4-arm-a+ordinal, digest_settled=false, gist_blocks=0` (verbatim turn-1 recall, digest-independent).
- Non-ordinal → `strategy=v4-arm-a+predigest, digest_settled=false` (unsettled-digest read now visible).

### Huible repo

| Commit | What |
| --- | --- |
| `9d79d66` | `WorkingMemoryRecall` + `WorkingMemoryView` carry `digest_settled`/`gist_blocks` (None = not observed); `e0_turn34_recall.py` settle poll reads the envelope fields directly (header sniff = legacy fallback), records `pre_probe_settle` + `gist_blocks` in evidence. Persona suite 22 passed (3 new). |
| (same push) | `e0_replay_w6.py`: pre-probe gateway read records `pre_probe_settle` into the `turn34_recall` check before the turn-15 probe fires. |

huible-app image rebuilt and restarted 09:44Z (trace fields live).

## Acceptance replay attempt 1 (09-08 09:47Z) — FAIL: z.ai ceiling, NOT the relay

Evidence: `hu2687_e0w6_replay_0_ceilingfail_20260908.json` (conversation `e0w6-50956f772c`).

- Turns 12–17 (9 of 17) served the approved `[fake-llm:…]` fallback. App guard log: `zai daily token ceiling reached; refusing hosted call (limit=200000 tokens, day-to-date=200760 tokens, day=2026-09-08)`.
- The durable ledger shows the pool fully consumed two days running (`2026-09-07: 200201`, `2026-09-08: 200760`); ~150k was spent today before this run by other consumers.
- **Capture gate held under real filler**: 9 fake replies served → **0** `[fake-llm:` rows in the store (18 clean rows: 9 user / 9 assistant). Verified directly in `vectors.db.l0_conversations`.
- **Settle observability worked end-to-end**: `pre_probe_settle = {digest_settled: false, gist_blocks: 0, strategy: v4-arm-a+ordinal}` recorded before the probe fired — the probe consumed no digest-settled read and the ordinal path served it regardless.
- Turn-34 recall markers could not be graded (probe reply was filler) → verdict FAIL is a provider-budget artifact, not a relay regression. No pollution-reset tricks used; the attempt is kept as evidence.

## Acceptance plan (2026-09-08, superseded)

z.ai ceiling is per-UTC-day (resets 00:00 UTC). Three consecutive full E0 replays (`scripts/e0_replay_w6.py`) scheduled right after the 09-09 reset on one epoch, no resets between:

1. Wake via issue monitor (~00:10 UTC 09-09), check ledger day-to-date ≈ 0.
2. Run replays 1–3 back-to-back, each into `docs/evidence/hu2687_e0w6_replay_<n>.json`; verify `turn34_recall.pass` + `pre_probe_settle` present + verdict PASS ×3.
3. Gate: `digest_settled` may legitimately be false at probe position (block-0 race window) — the ordinal path is the designed mitigation; what must hold is the verbatim turn-1 recall and observable settle state.

## ACCEPTANCE RUN (2026-09-09 ~00:11–00:31 UTC) — MET: 3/3 consecutive PASS

One epoch (no app/relay redeploy since 09-08 09:44Z; no pollution-reset tricks; fresh session per replay; ledger checked between runs, ceiling never breached).

| Replay | Conversation | Verdict | turn34 reply (turn 15) | markers | pre_probe_settle |
| --- | --- | --- | --- | --- | --- |
| 1 | `e0w6-4ee8e1f1bb` | **PASS** (all 9 checks) | `Something devastatingly clever like "hey who r u?"` | hit | `digest_settled=false, gist_blocks=0, strategy=v4-arm-a+ordinal` |
| 2 | `e0w6-e21dfd01dd` | **PASS** | `Pretty sure it was "hey who r u?"` | hit | same, ordinal |
| 3 | `e0w6-e1c8d43231` | **PASS** (all 9 checks) | `Pretty sure it was "hey who r u?"` | hit | same, ordinal |

Evidence files: `docs/evidence/hu2687_e0w6_replay_{1,2,3}.json`.

- **Turn-34 recall deterministic ×3**: the verbatim turn-1 row is recalled every time via the zero-LLM ordinal path (`v4-arm-a+ordinal`) — ranking/digest race cannot touch it.
- **Settle state observable before the probe**: `pre_probe_settle` recorded by the rig (direct gateway read) before the turn-15 probe fired in all three runs. `digest_settled=false` at probe position is the known block-0 race window and is exactly the case the ordinal path was built to serve.
- **Ledger**: 2026-09-09 day-to-date finished at 164,587/200,000 tokens (3 replays ≈ 124k); no `[fake-llm:]` filler served in any run.
- Footnote: an unplanned 4th replay at 00:11 UTC (rig `--help` has no argparse and ran the suite; conversation `e0w6-817ac0662e`) also recalled verbatim + settle observed, but full verdict FAIL on `sitcom_wall` (turn-12 "Matt LeBlanc / actor" pierce) — an HU-2472 app-side micro-tell gate, not relay scope; not booked as acceptance evidence (output not captured to file). All three booked replays pass `sitcom_wall`.

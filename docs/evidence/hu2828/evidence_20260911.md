# HU-2828 evidence — Chandler tools: time-of-day + real-world search (2026-09-11)

Code: commits `02c66a1` (feat), `9545d7b` (r1), `ccf3c4e` (r2), `3b51d27` + test-fix
(r3) on `w1-local-onnx-embeddings`. Engine redeployed after each revision
(`docker compose build app && up -d`).

## Tool-layer deliverables — DONE

1. **Time-of-day resolved to the persona's location (Chandler = NYC).**
   - `persona/tools.py`: `location_timezone` metadata + `resolve_persona_tz`;
     `in_world_now(..., tz=)` converts the carried time-of-day to the
     persona's zone; `caretaker_reply(..., tz=)` renders the real clock in
     the persona's zone.
   - HU-2774 `"noon"` pin is now scoped to machine flows: human portals send
     `X-Huible-Client: portal-human` (portal.py updated + restarted) and the
     clock carries the real persona-local time. Machine flows (battery,
     r10 one-shot armed for ~2026-09-12) keep the pin byte-identically.
   - Live PASS (3 portal runs): caretaker turns answered "17:42 EDT",
     "17:48 EDT", "18:04/18:05 EDT" — correct NYC time, was UTC/noon before.

2. **Empty LLM content is retryable; never a raw 500.**
   - `llm/client.py`: `LLMEmptyContentError` (LLMError subclass) raised on
     empty content; `ZaiLLMClient.generate` retries x2 with short linear
     backoff, per-attempt usage metering, structured
     `zai.empty_content_retry conversation=...` logs.
   - `api/app.py`: one extra turn-level retry for no-text tool turns; any
     residual `LLMError` degrades to structured **503**
     `{detail:{error:{code:LLM_TRANSIENT, retryable:true, message,
     conversation_id, trace_id}}}` — the portal renders `detail.error.message`
     verbatim, never "[500] engine error". Trace id is minted BEFORE
     generation so failure paths log it.
   - DONE-WHEN test: `tests/api/test_chat_realworld_and_transient.py::
     test_forced_empty_response_recovers_without_500` (200 + normal reply)
     and `tests/llm/test_zai_empty_content_retry.py` (recover / exhaust /
     no-retry-for-malformed). 105 tests green in touched suites.

3. **Lane-routed search + persona-scoped knowledge profile.**
   - `persona/realworld.py`: `PersonaKnowledgeProfile` from
     `metadata.search_lanes` (fail-closed, only explicit `true` enables),
     `derive_search_lanes(profile_text)` for provisioning from *dialog
     evidence* (Chandler corpus: rent/apartment/Knicks → current_reality ON,
     academic OFF; Ross profile → academic ON), shape classifiers,
     SearXNG JSON client (single vendor-facing function), snippet filter
     (≥40 chars — drops navigation boilerplate), query templates
     (rent/weather/game/coffee/pizza + location label).
   - Chandler DB metadata set: `location_timezone=America/New_York`,
     `location_label=Greenwich Village, New York`,
     `search_lanes={current_reality:true, academic:false}`.
   - Guard integration: notes ground claims via `external_context` in
     alignment + capability guards (HU-2070 widening precedent); wall stays
     silent on lane-fired turns.
   - Live logs (portal-d1761aa..., portal-9504..., final run): the lane
     fired on rent/neighborhood/weather/game and **skipped the Mayans probe
     (`realworld.lane_skip ... lane=academic provisioned=false`)** — correct
     honest-ignorance posture on all 3 live runs (transcripts in
     `runs/hu2828/transcript.json`, 23 turns each, 23/23 HTTP 200).

## Search-backed answer clause — BLOCKED on backend health (external)

- The named backend (self-hosted SearXNG :8888) answers HTTP 200 but its
  engine pool is unusable from this datacenter IP: the Aug 15 allowlist
  (bing, qwant) has decayed to captcha/timeout junk (docker logs show
  repeated `SearxEngineCaptchaException` for qwant; bing returns
  off-topic boilerplate), duckduckgo 403s (per the config comment).
- Ops fix applied this heartbeat: added **brave, mojeek, yahoo,
  duckduckgo** to `/opt/searxng/settings-main.yml` (backup:
  `settings-main.yml.bak-hu2828-20260911`) and restarted the container.
  **Brave immediately returned real, usable figures** for the exact tool
  query ("average rent greenwich village new york" → "$6,100 average /
  $4,650 studio / $6,500 1BR / $8,200 2BR") — proving the end-to-end path —
  but decays to CAPTCHA within minutes of use (54 captcha events in the
  final 40-minute window).
- Consequence: in the live portal runs the rent probe answers from canon
  ("rent controlled") because the notes carry no usable facts (correct
  degradation — the persona never invents figures when notes are empty;
  the tool layer filtered the junk out and would ground a usable snippet).
- **Unblock needed (infra decision, founder/JARVIS):** route SearXNG
  engine egress through a residential/tailnet relay, or approve a paid
  SERP API. The tool layer needs only `SEARXNG_BASE_URL` (or the single
  `searxng_search()` function) swapped — vendor-replaceability doctrine
  holds. Once stable, rerun `runs/hu2828/probe_conversation.py` —
  expected: rent answer cites searched figures.

## Artifacts

- `runs/hu2828/probe_conversation.py` — the 23-turn stranger probe
  (time probe, rent probe, Mayans probe).
- `runs/hu2828/transcript.json` — final run transcript (3 runs archived in
  git history; this file is the last).
- `runs/hu2828/engine_realworld_and_retry.log` — lane fire/skip + trace-id
  log lines from the final window.
- `runs/hu2828/searxng_captcha_count.txt` — backend decay evidence.
